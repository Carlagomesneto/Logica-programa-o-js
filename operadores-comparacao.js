const numero  = valor;

// Operadores de comparação
// == é igual
// != não é igual
// > maior que
// < menor que 
// >= maior OU igual
// <= maior OU igual
// === é estritamente igual (valor E tipo)
// !== não é estritamente 


// true -> palavra reservada para o valor "verdadeiro"
// false -> palavra eservada para o valor "falso"
// -> "NÃO"

// Estrutura IF-ELSE

const a  = 'a', b = 'A';
const condicao = a == b;

if (condicao) {
    console.log(`VERDADEIRO`);
} else {
    console.log(`FALSO`);
}

